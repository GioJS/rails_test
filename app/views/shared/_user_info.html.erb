<%= link_to gravatar_for(current_user, size: "50"), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t('show_profile'), current_user %></span>
<span><%= t('microposts') %> (<%=microposts.length %>)</span>